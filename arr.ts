// ARRAY AND OBJECTS


// let names = ['name','phương','toán'];
// // names.push('sơn');
// // // names.push(4);
// names.push(false);


// let number = [1,2,3];
// number.push(1);
// number.push(2);
// // // number.push('adaa');
// // number[3] = 'henru';

// let mixed = [1,'a',false];
// mixed.push(3);
// mixed.push('henry');
// mixed.push({a:'2',b:'4'});


let person = {
    name : 'phương',
    age : 20,
    isSV : true
}
// person.name = 5;
person.name = 'demo';

// person.age = 'sdaa';
person.age = 1;

// person.hobby = 'sdasda';

person = {
    name : 'Demo',
    age : 1,
    isSV : false
}